# Week-1
Design a deep learning-based system to detect forest fires using image classification techniques. The system should analyze images and classify them into "Fire" and "No Fire". By leveraging a convolutional neural network(CNNs), the model aims to provide accurate fire detection, assisting in early intervention and disaster prevention.
